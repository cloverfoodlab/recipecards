const defaultState = {
  recipes: [
    "recipe1",
    "recipe2"
  ]
}

const recipes = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default recipes
